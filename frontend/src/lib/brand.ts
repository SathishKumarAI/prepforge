/**
 * The product's name, once. The sidebar brand block, the breadcrumb, the page
 * eyebrow and the document title all read it from here, so a rename is one
 * edit and can never leave the tab saying one thing and the nav another.
 *
 * "Recall" replaced "PrepForge" on 2026-09-07 (COD-154): the name should say
 * what the app does. The repo, package and docs keep the old name until a
 * docs pass — this constant is the UI's only source.
 */
export const APP_NAME = "Recall";
export const APP_TAGLINE = "AI/ML interview prep";
