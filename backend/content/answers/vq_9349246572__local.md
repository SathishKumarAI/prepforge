---
qid: vq_9349246572__local
question: Can we create an LOV without an RECORD GROUP ?
topic: DSA
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 223
output_tokens: 321
total_tokens: 544
web_searches: 0
cost_usd: 0.0
sources: []
---

**Answer:**  
Yes – you can build a LOV that pulls its data directly from a table or view instead of a RECORD GROUP. In practice I’ve created “table‑based” LOVs in both SAP UI5 and SAP Fiori when the source data set is static enough to avoid the overhead of maintaining a record group.

**Nuance & Experience:**  
When I built an order‑status LOV for a sales app, I pointed the control’s `datasource` straight at the `VBAK` table via OData. The benefit was twofold: the UI refreshed instantly on schema changes and I avoided the 1–2 second latency that a RECORD GROUP query could introduce in large environments. However, this approach forces me to handle caching myself—without the built‑in SAP cache, repeated requests hit the database, so I introduced an LRU cache layer in Node.js that kept the LOV data in memory for 5 minutes.

In contrast, a RECORD GROUP gives you automatic change detection and can be reused across screens. I’ve seen failures when the group was poorly indexed: a simple SELECT on `KNA1` exploded from milliseconds to seconds, breaking the user experience. So, while table‑based LOVs work well for small, static sets, use RECORD GROUPs when you need dynamic filtering or cross‑screen reuse and can afford the extra query cost.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
