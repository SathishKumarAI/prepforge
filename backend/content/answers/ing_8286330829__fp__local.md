---
qid: ing_8286330829__fp__local
question: 'Explain: Glean API Tokens — About Glean MCP server'
topic: Machine Learning
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 172
output_tokens: 387
total_tokens: 559
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-05T20:42:54-05:00'
sources: []
---

**Glean API Tokens – why they exist and how they work**

At its core, a *Glean* service is a **stateful query engine** that runs on the *MCP (Machine‑to‑Client Protocol) server*. The MCP server exposes a REST‑like interface to clients (browsers, mobile apps, bots). Every request must prove that it originates from an authorized user or system.  
The token is the minimal cryptographic artifact that satisfies this proof while remaining lightweight for high‑throughput traffic.

1. **Fundamental problem** – The server must authenticate *before* evaluating a query to avoid leaking data and to enforce per‑user quotas.  
2. **Why a JWT‑style token works** – A JSON Web Token (JWT) bundles the user’s identity, permissions, and an expiry timestamp in one signed blob. The MCP can verify the signature locally without round‑trips to an auth server, preserving latency.  
3. **Deeper principle: locality of reference** – By embedding the policy inside the token, the query planner can immediately prune indexes that a user cannot see, reducing search space and memory pressure.  
4. **Non‑obvious insight** – Tokens are *short‑lived* (typically 5–15 min) to mitigate replay attacks, yet they contain a deterministic “query hash” of the last executed request. If an attacker replays a token with a different query, the server will reject it because the embedded hash no longer matches, effectively turning the token into a *one‑time credential* for that exact operation.

Thus, Glean API tokens solve authentication, authorization, and efficient query planning in a single, stateless, verifiable object.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
