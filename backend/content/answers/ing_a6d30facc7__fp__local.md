---
qid: ing_a6d30facc7__fp__local
question: Your agent reads screenshots and PDFs supplied by users. How do you defend
  against instructions hidden inside images?
topic: AI
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 179
output_tokens: 486
total_tokens: 665
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-05T22:00:16-05:00'
sources: []
---

**From first principles**

When a user uploads an image or PDF, the *problem* is to decide whether the visual content contains **legitimate information** (e.g., a photo of a document) or **malicious hidden data** (steganographic instructions).  
The solution must respect two constraints:

1. **Information preservation** – we cannot corrupt useful data for legitimate users.  
2. **Security** – any covert payload that could influence the agent’s behavior must be caught.

We therefore treat the image as a noisy channel with *signal* + *payload*. By Shannon’s theorem, the maximum information rate is bounded by the channel capacity; any excess entropy beyond what is explainable by natural variation signals hidden data.  
**Detection pipeline**

| Step | Rationale |
|------|-----------|
| **Pre‑processing & noise estimation** | Compute local variance and histogram statistics to model expected image noise. |
| **Steganalysis features** | Extract LSB, DCT, or wavelet residuals; compare entropy against a learned distribution of clean images. |
| **Cross‑modal consistency** | OCR the visible text, compare it with any textual content in the PDF’s metadata and with the user’s own description. Discrepancies flag potential manipulation. |
| **Adversarial training** | Fine‑tune a CNN on synthetic stego images generated by common encoders; the network learns to classify “clean vs. covert.” |

**Non‑obvious insight**

Even if an image passes statistical tests, *contextual mismatch* can betray hidden instructions: for example, a photo of a whiteboard that contains no text but is accompanied by a PDF with complex code in its metadata. A lightweight *semantic consistency module* (e.g., CLIP embeddings) can detect such incongruities far earlier than pixel‑level tests.

By combining **entropy‑based steganalysis** with **cross‑modal semantic checks**, the agent robustly defends against hidden instructions while preserving legitimate user content.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
