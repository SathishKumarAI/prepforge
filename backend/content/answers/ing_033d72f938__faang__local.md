---
qid: ing_033d72f938__faang__local
question: 'Explain: SLM Distillation — Reranking Strategies'
topic: AI
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 233
output_tokens: 458
total_tokens: 691
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-06T18:51:27-05:00'
sources: []
---

**Clarify**  
The interview asks about *SLM distillation* (Small‑Language‑Model distillation) and how one can use *reranking strategies* during inference. I’ll assume:  
1. A large teacher LLM is available; the student is a smaller, faster model.  
2. Distillation uses the teacher’s logits or hidden states as supervision.  
3. Reranking refers to selecting the best hypothesis from a set generated by the student.

**Approach**  
1. Train the student with standard distillation loss (cross‑entropy on teacher logits + MSE on intermediate representations).  
2. At inference, let the student generate multiple candidate outputs (beam or top‑k sampling).  
3. Rerank those candidates using a lightweight scorer that approximates the teacher’s quality—e.g., by feeding the teacher a *compressed* version of each candidate and comparing its logit distribution to the student's.

**Depth**  
- **Distillation loss:** `L = α KL(student_logits || teacher_logits) + (1−α) MSE(student_hidden, teacher_hidden)`; choose α ≈ 0.5.  
- **Reranker design:** a small feed‑forward network or even the teacher itself with a truncated context to score each candidate’s probability. Complexity: O(n_candidates × d) where *d* is model dimension; negligible compared to full decoding.  
- **Benefits:** mitigates hallucinations, improves BLEU/ROUGE, and keeps inference fast because reranking is shallow.

**Edge Cases**  
- If the teacher is noisy, the reranker may reinforce errors—test on a held‑out validation set with human judgments.  
- Too few candidates → no improvement; too many → latency spike—balance beam width.  

**Optimize & Communicate**  
Explain that this pipeline keeps the student lightweight while leveraging the teacher’s knowledge at inference time. Highlight trade‑offs: extra GPU passes for reranking vs. higher output quality, and how to tune candidate count or reranker depth based on latency constraints. This demonstrates structured reasoning, clear communication, and a technically sound solution.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
