---
qid: ing_2df64f01fe__star__local
question: Your chatbot starts losing the thread after about ten turns. What are your
  options for managing conversation history?
topic: AI
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 245
output_tokens: 333
total_tokens: 578
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-05T00:00:21-05:00'
sources: []
---

**Situation:**  
I was building an AI‑powered customer support chatbot for a telecom client. After about ten user–bot exchanges the bot would start repeating earlier responses or lose context entirely, causing frustrated users and high ticket escalation rates.

**Task:**  
I needed to keep the conversation coherent across longer sessions without blowing up memory usage or response latency, while still allowing the model to understand user intent accurately.

**Action:**  
First, I implemented a sliding‑window buffer that kept only the last 8–12 turns in a structured JSON format and fed this as context to the LLM. For critical information (e.g., account numbers, problem categories) I extracted key entities with an NER pipeline and stored them in a lightweight key‑value store; these were re‑injected into prompts when needed. To reduce token cost, I used concise summary vectors generated by a sentence‑embedding model to compress older turns into a single “conversation gist” token that the LLM could reference. Finally, I added a fallback rule: if the bot’s confidence dropped below 0.4 after five turns, it would ask clarifying questions rather than proceeding.

**Result:**  
User satisfaction scores rose from 72 % to 88 %, and escalation rates fell by 35 %. The system now runs within our token budget (≈1,200 tokens per turn) and latency stays under 800 ms. I learned that balancing explicit context with semantic compression is key for scalable conversational AI.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
