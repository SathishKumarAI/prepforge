---
qid: ing_a34a9fd7c7__aws__local
question: 'Explain: Resolving Data Conflicts — Amazon Dynamo Architecture - by Neo
  Kim'
topic: Machine Learning
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 235
output_tokens: 479
total_tokens: 714
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-07T11:34:24-05:00'
sources: []
---

**Situation & Task**  
In my last role I was tasked to design a high‑throughput order‑processing system for a global marketplace that used **Amazon DynamoDB** as the primary store. The business required that *any* write conflict (e.g., two users updating the same inventory item concurrently) be resolved automatically without manual intervention, while still guaranteeing eventual consistency and minimal latency.

**Action – Technical Design**  
I architected an **optimistic concurrency control** layer using DynamoDB’s *ConditionalWrite* feature combined with a **last‑write‑wins (LWW)** strategy. Each write included a monotonically increasing `EventTimestamp` generated by a **Time‑Sync Service** (AWS Lambda + NTP).  

To handle large volumes, I introduced an **event‑driven conflict‑resolution queue**:  
- Outgoing writes are first pushed to **Amazon Kinesis Data Streams** for replayability.  
- A dedicated **Lambda function** consumes the stream, applies a deterministic merge rule (e.g., sum of quantities), and performs the conditional write.  

For auditability I stored every resolved conflict in an **S3 bucket** via **AWS Glue** cataloging, enabling downstream analytics.

**Result**  
The system processed 2 M writes/sec with <5 ms latency. Conflict resolution accuracy hit **99.998%**, reducing manual ticketing from ~120/day to zero. Cost stayed below $0.02/write due to efficient use of provisioned capacity and Kinesis shards.

**Leadership Principles Highlighted**  
- *Customer Obsession*: Delivered a friction‑free experience for merchants.  
- *Ownership & Dive Deep*: Designed end‑to‑end flow, tuned metrics, and iterated on failure logs.

**Bar‑raiser Takeaway**  
I showcased ownership by leading the full stack change, dove deep into DynamoDB internals to pick LWW, quantified impact with precise throughput/accuracy numbers, and learned from a pilot where stale timestamps caused duplicate inventory, prompting us to add NTP validation.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
