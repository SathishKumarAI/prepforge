---
qid: ing_3458bbcc47__star__local
question: 'Explain: On-Policy Distillation - Thinking Machines Lab'
topic: Machine Learning
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 237
output_tokens: 378
total_tokens: 615
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-05T00:12:58-05:00'
sources: []
---

**Situation:**  
In my last role at a fintech startup we were building an automated trading bot that used reinforcement learning (RL) to learn optimal order placement strategies. The production model was a deep Q‑network running on GPU clusters, but it suffered from high variance and slow convergence when the market conditions changed.

**Task:**  
I needed to create a more stable, deployable policy that could be served with low latency while preserving most of the RL agent’s performance. The goal was to reduce inference time by an order of magnitude and cut down on GPU usage for real‑time trading.

**Action:**  
I implemented an on‑policy distillation pipeline: first I collected a large replay buffer of state–action pairs generated by the live RL policy during normal market hours. Then, using a lightweight feed‑forward network as the student, I trained it with supervised learning to mimic the teacher’s action probabilities (cross‑entropy loss) while also adding a KL regularizer to keep the student close to the teacher’s distribution. To maintain on‑policy fidelity, I refreshed the replay buffer every 6 hours so the student kept up with recent market dynamics. We deployed the distilled model on an edge TPU, reducing inference latency from ~200 ms to <5 ms.

**Result:**  
The distilled policy achieved a 92% hit rate compared to the RL baseline’s 95%, and trading throughput increased by 7× while GPU costs dropped by 80%. I learned that careful sampling of on‑policy data and a small regularization term can bridge the gap between high‑variance RL agents and low‑latency production models.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
