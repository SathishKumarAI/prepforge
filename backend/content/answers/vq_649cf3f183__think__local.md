---
qid: vq_649cf3f183__think__local
question: How to you create Singleton classes in Scala?
topic: Data Engineering
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 188
output_tokens: 422
total_tokens: 610
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-06T01:41:24-05:00'
sources: []
---

**1. Clarify the goal & assumptions**  
   - Understand that “singleton” means a class with exactly one instance per JVM.  
   - Assume we’re working in Scala 2.x or 3.x and want a thread‑safe, lazy‑initialized instance.  

**2. Pick a mental model / pattern**  
   - Use the *object* keyword: it compiles to a final class plus a static INSTANCE field.  
   - Alternatively, for more control (e.g., parameterized construction), use the *lazy val* inside a companion object or the *static‑block* idiom.  

**3. Step‑by‑step reasoning**  
   1. Decide if you need parameters:  
      - No → simple `object MySingleton { … }`.  
      - Yes → create a class with private constructor and expose an instance via `lazy val` in its companion object.  
   2. Ensure thread safety: `lazy val` is safe; the JVM guarantees double‑checked locking semantics.  
   3. Consider lazy vs eager init: use `object` for eager, or add `@volatile var instance = ...` if you need custom initialization logic.  

**4. Common traps to avoid**  
   - Forgetting that Scala objects are *already* singletons; don’t wrap them in another singleton pattern.  
   - Using a plain `val` instead of `lazy val` when the constructor has side effects—this forces eager init and can cause deadlocks.  
   - Mixing Java‑style static fields with Scala objects, leading to confusing API surface.  

**5. Sanity‑check & communicate**  
   - Verify that the generated bytecode contains a single instance field (`MODULE$`).  
   - Explain to the audience: “In Scala, `object` is the idiomatic singleton; if you need parameters, use a companion object with a private constructor and expose a lazily initialized value.”

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
