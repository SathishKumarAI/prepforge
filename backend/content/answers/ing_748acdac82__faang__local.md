---
qid: ing_748acdac82__faang__local
question: 'Q128: EU AI Act Article 50 became enforceable on August 2 with fines up
  to 3% of global turnover, and California''s transparency law took effect the same
  day. You ship a product that generates text and images in both markets. What do
  you build?'
topic: AI
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 278
output_tokens: 646
total_tokens: 924
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-06T23:15:20-05:00'
sources: []
---

**Clarify**

We’re shipping a generative‑AI tool (text & image) to EU and California.  
- EU: **EU AI Act Art 50** – requires high‑risk systems to have risk management, documentation, human oversight, data governance, and an external audit trail.  
- CA: **Transparency law** – mandates that any system that can generate content must clearly disclose it is AI‑generated and provide a simple way for users to request the source model and training data.

Assumptions I’d confirm:
1. The product falls under “high‑risk” in the EU (e.g., used for employment, finance).  
2. We ship identical binaries; no separate codebases per jurisdiction.  
3. Users can be identified but not tracked beyond a session.  

**Approach**

1. **Unified compliance layer** – a runtime policy engine that enforces both regimes.  
2. **Audit‑ready logging** – immutable logs of model inputs, outputs, and user context for EU risk docs.  
3. **Transparent UI flag** – automatically prepend a “Generated by AI” banner with a link to the model & data summary.  

**Depth**

- *Risk management*: implement a lightweight risk assessment module that evaluates each request against predefined criteria (e.g., content type, user role). If high‑risk, route through a human‑in‑the‑loop review queue; otherwise auto‑approve.  
- *Documentation*: store metadata (model version, training data snapshot hash, fine‑tuning parameters) in a secure, versioned datastore. Expose an API endpoint that returns the current “source model” and “training data” for CA disclosure.  
- *Audit trail*: use append‑only, tamper‑evident logs (e.g., WORM storage or blockchain‑style Merkle trees). Log: timestamp, user ID, request payload hash, output hash, risk score, audit decision.  
- *Human oversight*: integrate a lightweight dashboard for reviewers; notifications on high‑risk outputs.  

Complexity: O(1) per request overhead; log writes are amortized over batch size. Trade‑off is the extra latency of policy checks and potential reviewer bottleneck.

**Edge Cases**

- Users bypass the UI (e.g., via API). We enforce disclosure at the API level by adding a mandatory header `X-AI-GENERATED: true`.  
- Model updates mid‑flight – we tag every output with the current model hash to satisfy audit requirements.  
- Non‑high‑risk but sensitive data (medical) – fallback to manual review.

**Optimize & Communicate**

To reduce latency, cache risk scores for common prompts and pre‑compute model metadata snapshots. I’d present this as a “Compliance‑as‑a‑Service” layer that sits between the user request and the generative engine, ensuring both EU Art 50 and CA transparency are met without duplicating codebases. This architecture scales linearly with users, keeps audit logs immutable for legal defensibility, and provides clear UI signals to satisfy California’s disclosure mandate.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
