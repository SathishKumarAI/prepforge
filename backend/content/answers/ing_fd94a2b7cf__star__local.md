---
qid: ing_fd94a2b7cf__star__local
question: 'Explain: Idempotency — What is an API? | System Design'
topic: Machine Learning
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 240
output_tokens: 337
total_tokens: 577
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-05T07:56:01-05:00'
sources: []
---

**Situation** – While leading the redesign of our customer‑onboarding microservice, we noticed that repeated POST requests from a flaky mobile app caused duplicate user records and inflated billing data. The service exposed a REST API that clients could call any time to create an account.

**Task** – I needed to make the “create user” endpoint idempotent so that regardless of how many times the client hit it, only one record would be created, preserving data integrity and cost accuracy.

**Action** – First, I added a unique `client_id` header generated by the app. On the server, I stored this ID in a Redis cache with a TTL matching the request window. When a POST arrived, the handler checked if the key existed: if yes, it returned the cached user ID; if no, it performed validation, created the record, and then wrote the `client_id → user_id` mapping to Redis atomically using a Lua script to avoid race conditions. I also switched the underlying database write to an UPSERT operation (ON CONFLICT DO NOTHING) to guard against duplicate inserts from simultaneous requests.

**Result** – After deployment, duplicate account creation dropped from 12% of all POSTs to <0.1%. Billing errors fell by 95%, and we saved roughly $18k per month in avoided over‑charges. I learned that idempotency is not just a theoretical concept; implementing it with proper caching and database guarantees can dramatically improve system reliability and cost control.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
