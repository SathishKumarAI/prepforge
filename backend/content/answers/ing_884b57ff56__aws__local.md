---
qid: ing_884b57ff56__aws__local
question: 'Explain: 5.4 Get a Post by ID — Design Instagram | System Design Interview
  | AlgoMaster.io'
topic: Machine Learning
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 242
output_tokens: 605
total_tokens: 847
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-07T10:32:20-05:00'
sources: []
---

**Situation & Task (Customer Obsession)**  
I was asked to design the “Get a Post by ID” endpoint for an Instagram‑style app that could serve 200 M active users daily. The goal was to return a post with its image, caption, author metadata, and top comments in <300 ms while keeping cost under $5k/month.

**Approach & Technical Design (Dive Deep + Invent & Simplify)**  
1. **API Layer** – API Gateway → Lambda@Edge for routing; 30 % of traffic is cached at edge with a TTL of 60 s to reduce origin load.  
2. **Post Retrieval** – DynamoDB Global Secondary Index on `post_id` (partition key) + `timestamp`. Reads are strongly consistent, 10 µs latency.  
3. **Image & Video** – Stored in S3 (object key = `<user_id>/<post_id>.<ext>`). Signed URLs generated by Lambda; CloudFront edge cache for 24 h to serve media with <50 ms latency.  
4. **Comments** – Stored in a separate DynamoDB table (`comments`) partitioned by `post_id`. A single query returns the top‑10 comments (sorted by likes) using an index on `(post_id, like_count)`; pagination handled via exclusive start key.  
5. **Author Metadata** – Fetched from a read‑optimized PostgreSQL RDS instance via Aurora Serverless with a 5 second timeout fallback to cached author info in ElastiCache Redis.

**Scalability & Cost (Deliver Results)**  
- *Read traffic*: 200 M users × 10 req/s ≈ 2B requests/month. DynamoDB provisioned throughput at 50k RCU, auto‑scaling keeps cost ~ $1k.  
- *Media delivery*: CloudFront handles >95% of bandwidth; S3 + Glacier cold‑archive for infrequently accessed media saves another $0.5k.  
- *Lambda invocations*: <100 µs per call; 30 % edge cache reduces origin hits, saving ~$1.2k.  
Total projected cost: ~$4.7k/month with 99.999% availability.

**Result (Bias for Action)**  
Implemented in 3 sprints; latency dropped from 750 ms to 210 ms (72% improvement). A/B test showed a 15% increase in post engagement due to faster load times.  

**Learning & Failure**  
Initial design used a single DynamoDB table for posts and comments, causing hot partitions during viral events. After observing throttling, I split them into separate tables and introduced read‑replicas, learning the importance of *dive deep* into access patterns before scaling.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
