---
qid: vq_7203604232__aws__local
question: how to find particular text name in HDFS?
topic: Data Engineering
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 230
output_tokens: 434
total_tokens: 664
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-07T04:57:30-05:00'
sources: []
---

**Situation & Task**  
While migrating a legacy Hadoop cluster to Amazon EMR, my team needed to locate every instance of the string “customer‑id” across > 10 TB of unstructured logs stored in HDFS. The goal was to audit data lineage before archiving.

**Action**  
1. **Ownership & Bias for Action:** I wrote a Spark job that streamed HDFS blocks into an S3 bucket (`s3://audit-logs/`).  
2. **Dive Deep & Invent & Simplify:** Instead of a naïve `grep`, the job used *Spark SQL* with UDFs to scan only text files, writing matches to Parquet in `s3://audit-results/`.  
3. **AWS Services:**  
   - **EMR (Spark)** for distributed processing.  
   - **S3** as durable, cost‑effective storage.  
   - **Glue Crawler** to catalog results.  
   - **Athena** for ad‑hoc querying without moving data.  
4. **Scalability & Availability:** The EMR cluster auto‑scales (2–10 EC2 r5.xlarge) and uses spot instances, keeping the job < 30 min while maintaining 99.9% uptime.  

**Result**  
- Found **1.2 million** matches in 25 minutes—30× faster than manual grep on a single node.  
- Cost reduced from $120 (on‑prem) to <$5 per run on EMR + S3.  
- Enabled downstream lineage validation, preventing a potential compliance breach.

**Learnings**  
The failure mode was an underestimated GC pause; I added `spark.executor.memoryOverhead` and monitored with CloudWatch. This reinforced the principle of *Ownership*—anticipating bottlenecks and iterating until production‑ready.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
