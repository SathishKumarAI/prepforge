---
qid: ing_8555951197__fp__local
question: You have dozens of ranking signals and a brand-new tenant with zero interaction
  data. How do you rank, and how do you improve?
topic: AI
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 186
output_tokens: 539
total_tokens: 725
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-05T20:48:32-05:00'
sources: []
---

**From first principles**

The core problem is *predicting relevance* for a user‑tenant pair that has never seen any interaction data.  
We can formalise it as estimating the expected reward \(R_{ui}\) for item \(i\) and user \(u\). When no clicks exist, the only source of information is the **prior** over items induced by the ranking signals.

Let each signal be a feature vector \(\mathbf{x}_i\) (e.g., freshness, category match, popularity). We assume a linear reward model  
\(R_{ui} = \boldsymbol{\theta}^\top \mathbf{x}_i + \varepsilon\).  
Without data we set \(\boldsymbol{\theta}\) to the *posterior mean* of a prior \(p(\boldsymbol{\theta})\), typically derived from the aggregate behaviour across all tenants. This is equivalent to **Bayesian regularisation**: the prior encodes what *should* be true when evidence is missing, preventing overconfident but biased predictions.

**Why it must work**

1. **Optimisation bias–variance trade‑off** – a flat prior gives high variance (unstable ranking), while an informative prior reduces variance at the cost of bias; the Bayesian posterior balances both.
2. **Information geometry** – the prior defines a manifold on which the model lives; the observed data from other tenants act as a Riemannian metric that shrinks uncertainty for new tenants along directions with high inter‑tenant similarity.

**Improvement loop**

1. Collect the first interactions (even a single click) and update \(\boldsymbol{\theta}\) via Bayesian updating or incremental gradient descent.
2. Re‑rank items using the updated posterior mean; expose diverse signals to encourage exploration.
3. As more data accumulates, gradually *shrink* the prior influence—this is automatic in Bayesian inference (the likelihood dominates).

**Non‑obvious insight**

Treat each tenant as a **parameterised sub‑task** in a multi‑task learning setting: instead of fitting one global model, learn a tenant‑specific offset \(\boldsymbol{\delta}_u\) with hierarchical regularisation. The zero‑data case then defaults to the shared global weights, but the system is ready to absorb tenant idiosyncrasies immediately when the first few clicks arrive. This hierarchical prior is often missed because practitioners lump all tenants together and ignore the natural grouping structure that drastically speeds convergence for cold starts.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
