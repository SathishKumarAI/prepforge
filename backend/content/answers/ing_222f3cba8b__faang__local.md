---
qid: ing_222f3cba8b__faang__local
question: 'Explain: Two-tier human confirmation — Computer Use Agent Production'
topic: AI
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 233
output_tokens: 555
total_tokens: 788
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-06T20:04:10-05:00'
sources: []
---

**Clarify**

The question asks for a design that uses *two‑tier human confirmation* in an AI system that produces *Computer Use Agents (CUAs)*—software agents that autonomously execute tasks on behalf of users.  
Assumptions to confirm:  
1. CUAs are generated by an LLM or planner and then deployed to perform actions.  
2. “Human confirmation” means a human must approve the agent’s plan **before** it runs, and again after each critical step (or at defined checkpoints).  
3. The system operates in safety‑critical domains (finance, healthcare, etc.) where mis‑execution is costly.

---

**Approach**

1. **Tier 1 – Plan Review:** After generation, the CUA’s action plan is rendered into a concise, human‑readable summary and sent to an operator for approval or edits.  
2. **Tier 2 – Runtime Checks:** While executing, the agent emits *checkpoint tokens* (e.g., “Will transfer \$10k to account X?”). Each token triggers an instant confirmation dialog; if denied, execution halts.  
3. **Audit & Feedback Loop:** All approvals and denials are logged; a learning module updates future plan templates to reduce redundant confirmations.

---

**Depth**

- **Plan Summary Generation:** Use a lightweight transformer fine‑tuned on “action → natural language” pairs, ensuring < 200 ms latency.  
- **Checkpoint Triggering:** Embed unique IDs in the agent’s code; upon reaching a checkpoint, send an API call to a Human‑In‑The‑Loop (HITL) service.  
- **Concurrency Handling:** Queue checkpoints per user; if multiple agents run concurrently, serialize confirmations to avoid overwhelming operators.  
- **Complexity:** O(1) for each confirmation lookup; overall system scales linearly with number of active agents.

---

**Edge Cases**

- *Denial during critical step*: Rollback or safe‑stop the agent.  
- *No human response*: Timeout policy (e.g., 30 s → abort).  
- *Adversarial prompts*: Sanitize inputs to prevent malicious checkpoint bypasses.

---

**Optimize & Communicate**

To reduce friction, we can pre‑train the CUA on “no‑op” checkpoints for non‑critical actions and batch confirmations. I’d present this as a layered safety net: first human vetting of intent, then runtime gatekeeping, ensuring transparency while preserving automation speed. This design balances efficiency with risk mitigation—exactly what FAANG interviewers value in robust AI system architecture.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
