---
qid: ing_ed336ef2d2__star__local
question: What metadata would you attach to each chunk, and what does it buy you?
topic: AI
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 241
output_tokens: 362
total_tokens: 603
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-05T07:23:09-05:00'
sources: []
---

**Situation:**  
At my previous company we were building a large language‑model‑driven chatbot that served both internal support tickets and external customer queries. The dataset was over 10 GB of unstructured logs, and our inference latency had to stay under 150 ms per request.

**Task:**  
I needed to design a chunking strategy that allowed the model to retrieve relevant context quickly while keeping the index size manageable. That meant deciding what metadata to attach to each text chunk and how it would accelerate retrieval.

**Action:**  
I added three key pieces of metadata: (1) **Document ID & timestamp** for provenance and version control; (2) **Semantic tags** generated by a lightweight clustering model on embeddings, giving us coarse topical buckets; and (3) an **inverted keyword index** built from the most frequent nouns/verbs in each chunk. These were stored in a vector database that also held the embedding vectors. During query time, I first filter chunks by semantic tag and timestamp window, then use the inverted index to narrow candidates before performing a cosine‑similarity search on embeddings. This multi‑layer filtering cut down the number of candidate vectors from 1 M to ~10 k per request.

**Result:**  
Latency dropped from 210 ms to 95 ms while maintaining 99.2% recall on a held‑out test set. Storage overhead increased by only 12 MB for the metadata tables, and we reduced GPU memory usage during inference by 35%. The exercise taught me that thoughtful metadata design can turn an otherwise unwieldy dataset into a fast, scalable retrieval system.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
