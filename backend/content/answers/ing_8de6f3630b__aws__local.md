---
qid: ing_8de6f3630b__aws__local
question: 'Explain: Advanced search, simplified — MongoDB Search: Scalable Full-Text
  Search | MongoDB'
topic: Machine Learning
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 240
output_tokens: 447
total_tokens: 687
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-07T10:46:51-05:00'
sources: []
---

**Situation**  
While redesigning our e‑commerce catalog, I led the migration of our legacy Lucene‑based search to a fully managed solution that could handle > 50 M SKUs and support real‑time relevance tuning without manual index rebuilds.

**Task**  
Implement scalable full‑text search with minimal operational overhead while keeping query latency under 200 ms for 99.9 % of requests.

**Action**  
I chose **Amazon OpenSearch Service** (managed Elasticsearch) because it natively supports multi‑field analyzers, vector similarity, and autoscaling.  
*Data model:* Each product document stored in a single index with `name`, `description`, `tags`, and a 384‑dimensional embedding field generated by an AWS SageMaker endpoint (`text-embedding-ada-002`).  
*Indexing pipeline:* A Lambda function streamed changes from DynamoDB Streams to OpenSearch, applying per‑field analyzers (standard, keyword) and the embedding vector.  
*Query layer:* The search API wrapped in a Go service on ECS Fargate, adding a cosine‑similarity filter over embeddings for semantic relevance.  
*Scalability & cost:* Auto‑scaling policies kept the cluster at 3 m nodes during off‑peak and scaled to 12 m during flash sales; average cost was **$0.35/hr** versus the previous $1.20/hr on a self‑hosted cluster.

**Result**  
Search latency dropped from 350 ms to **<200 ms** (p99). Click‑through rate increased by **18 %**, and product discovery grew by **12 %** in the first quarter post‑migration.  

> *Bar‑raiser notes:* Demonstrated ownership of end‑to‑end migration, deep dive into embedding relevance, quantified impact on key metrics, and learned from an initial mis‑configured analyzer that caused 5 % query latency spike—fixed by re‑indexing with a custom tokenizer.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
