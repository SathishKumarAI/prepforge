---
qid: ing_682379300f__star__local
question: 'Explain: Idempotency — Reliability | System Design'
topic: Machine Learning
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 236
output_tokens: 346
total_tokens: 582
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-05T01:55:43-05:00'
sources: []
---

**Situation:**  
At my previous company we built a real‑time recommendation engine that served personalized ads on a global CDN. During a major product rollout the ad‑serve API was hit with duplicate requests because of network retries, causing each user to see the same ad multiple times in quick succession and inflating click‑through metrics.

**Task:**  
I needed to guarantee that every ad‑delivery request produced a single, deterministic result regardless of how many identical calls arrived, without sacrificing latency or throughput.

**Action:**  
I introduced an idempotency key pattern at the API gateway: each client request carried a UUID generated by the front‑end. The backend stored the outcome in Redis with a 24‑hour TTL and a SHA‑256 hash of the payload as the key. On receipt of a request, the service first checked Redis; if a cached result existed it returned that immediately. If not, it executed the recommendation pipeline (feature extraction, model inference) and persisted the result before responding. I also added circuit‑breaker logic to avoid repeated expensive recomputations when upstream services were slow.

**Result:**  
Duplicate ad impressions dropped from 18% of traffic to <0.1%, restoring metric integrity. The overall latency stayed under 120 ms for 99th percentile requests, and we saved roughly 30% CPU usage during peak hours by avoiding redundant inference calls. I learned that idempotency is not just a safety net—it can be an efficiency lever when implemented with proper caching and key design.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
