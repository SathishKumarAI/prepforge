---
qid: ing_6bd807f369__aws__local
question: 'Explain: Semantic Similarity — Llm Evaluation'
topic: AI
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 228
output_tokens: 452
total_tokens: 680
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-07T09:27:50-05:00'
sources: []
---

**Situation / Task**  
While leading a research‑engineering sprint at my previous company, we were asked to build an internal benchmarking suite for large language models (LLMs). The core metric was *semantic similarity* – how closely two text snippets convey the same meaning. I owned this effort from requirement capture through production deployment.

**Action**  
1. **Define the metric**: Adopted cosine‑similarity over sentence embeddings, generated by a frozen Sentence‑BERT model fine‑tuned on our domain corpus.  
2. **Design pipeline**:  
   * Ingest raw pairs → AWS S3 batch → Lambda orchestrates Athena queries to generate embeddings via Amazon SageMaker endpoints (on-demand GPU).  
   * Store vectors in DynamoDB with GSI for similarity search, leveraging PartiQL for ad‑hoc cosine queries.  
   * Expose REST API through API Gateway + Lambda for real‑time scoring.  
3. **Scalability & Cost**: Used spot instances for SageMaker inference (≈ 30 % cost savings) and auto‑scaling on Lambda concurrency to keep latency < 200 ms per pair.  
4. **Validation**: Ran 100k human‑labeled pairs; achieved Pearson r = 0.87, surpassing the baseline of 0.81 (previous heuristic).  

**Result**  
- Cut model evaluation cycle from 2 days to 6 hours, enabling weekly releases.  
- Saved ~ $12K/month on inference costs via spot instances.  
- Earned a “Best Innovation” award and was invited to present at AWS re:Invent.

---

### Leadership Principles Highlighted
- **Ownership** – drove the project end‑to‑end, from concept to production.  
- **Dive Deep** – selected the right similarity metric, tuned embeddings, and quantified impact with Pearson correlation.  

*Bar‑raiser notes*: I demonstrated clear ownership, deep technical choices (embedding model, AWS services), quantifiable results, and learned from a failed prototype that used naive bag‑of‑words similarity—pivoting to sentence embeddings was the key turnaround.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
