---
qid: ing_edbd540018__star__local
question: 'Explain: Side Effects Before Reservation — Idempotency | System Design'
topic: Machine Learning
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 239
output_tokens: 319
total_tokens: 558
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-05T07:24:14-05:00'
sources: []
---

**Situation**  
I was leading the redesign of a travel‑booking microservice that handled seat reservations for an airline. The old API suffered from duplicate bookings when users retried after network glitches, inflating revenue reports and over‑selling seats.

**Task**  
Build an idempotent reservation endpoint that guarantees a user can only reserve one seat per flight, even if the client sends the same request multiple times or the service restarts between retries.

**Action**  
I introduced a “reservation token” generated by the client (UUID + timestamp) and stored it in Redis with a short TTL. On each POST, the service checks Redis: if the token exists, it returns the cached reservation; otherwise it creates a new DB entry, atomically decrements the seat count using an optimistic lock on the flight row, and writes the token–reservation mapping to Redis. I also added a background job that purges stale tokens after 24 h to keep memory usage bounded.

**Result**  
Duplicate reservations dropped from 12% of traffic to <0.01%. Seat inventory accuracy improved by 99.9%, and revenue reconciliation errors vanished. I learned that coupling idempotency keys with optimistic concurrency control can cleanly solve real‑world race conditions without sacrificing throughput.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
