---
qid: ing_9ee5ae3a61__fp__local
question: 'Explain: One Platform for Legal Work — Harvey | AI software for legal and
  professional services'
topic: AI
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 176
output_tokens: 386
total_tokens: 562
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-05T21:43:05-05:00'
sources: []
---

**One Platform for Legal Work – Harvey**

The fundamental problem in legal practice is *data silos*. Lawyers juggle case‑management systems, billing tools, document editors, research databases and client portals, each with its own data model and API. The cost of moving a single fact (e.g., a clause or a jurisdictional rule) across these silos is the latency that drives inefficiency.

Harvey solves this by **consolidating every workflow into one modular architecture**. At its core lies a *semantic graph* that represents facts, documents and actions as nodes linked by typed edges. This graph is the single source of truth; any change propagates instantly to every downstream component. The platform exposes three layers:

1. **Data layer** – unified schema with an extensible ontology for legal concepts.
2. **AI layer** – transformer‑based models that perform entity extraction, precedent matching and risk scoring directly on graph nodes.
3. **Interface layer** – context‑aware UI widgets (e.g., smart document editors) that surface relevant knowledge without leaving the task.

The deep principle is *information geometry*: by embedding legal facts in a high‑dimensional space, similarity queries become vector operations, drastically reducing search time and revealing latent relationships that traditional keyword searches miss.  

**Non‑obvious insight:** Because every fact is anchored to its provenance (court, jurisdiction, date), Harvey can perform **temporal reasoning**—predicting how a precedent’s weight may shift as statutes evolve—something most AI legal tools ignore. This foresight turns static knowledge bases into dynamic predictive engines.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
