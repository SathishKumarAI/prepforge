---
qid: ing_6149717184__fp__local
question: 'Explain: Build text-to-image search over 100M product images. Walk me through
  the design.'
topic: AI
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 179
output_tokens: 557
total_tokens: 736
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-05T19:32:15-05:00'
sources: []
---

**Goal:** Retrieve a set of product images that match a natural‑language query (e.g., “red leather backpack”) out of ~100 M candidates.

---

### 1. **Represent the data space**

* **Images → embeddings**  
  Use a vision encoder pretrained on large‑scale image–text pairs (CLIP, ALIGN). For every product image \(I_i\), compute a unit vector \(\mathbf{v}_i = f(I_i)\in\mathbb{R}^{d}\) (e.g., \(d=512\)).  
  *Why?* The encoder aligns visual and textual semantics; cosine similarity in this space approximates “how well the image satisfies the query”.

* **Text → embeddings**  
  For a query \(q\), obtain \(\mathbf{t}=g(q)\) with the same text encoder.  
  *Why?* Keeps both modalities in a shared metric, turning retrieval into a nearest‑neighbour search.

---

### 2. **Indexing for speed**

Build an approximate nearest neighbour (ANN) index over \(\{\mathbf{v}_i\}\):

1. **Product Quantisation (PQ)** or **HNSW** on the unit sphere.  
   *Why?* PQ compresses vectors to ~8 bytes each, enabling RAM‑based indices for 100 M items; HNSW offers sub‑millisecond queries with high recall.

2. Store metadata (product ID, price, etc.) alongside pointers for post‑filtering.

---

### 3. **Query pipeline**

1. Encode query → \(\mathbf{t}\).  
2. Retrieve top‑\(k\) nearest neighbours in the ANN index.  
3. Optional re‑ranking:  
   * Use a lightweight fine‑tuned transformer (e.g., CLIP‑fine‑tune on product captions) to compute exact similarity scores for the candidates.  
   * Apply business filters (price range, availability).

---

### 4. **Scalability & freshness**

* Periodically re‑compute embeddings as new images arrive or models improve.  
* Incrementally update PQ tables using online quantisation; HNSW can insert nodes on the fly.

---

### **Non‑obvious insight**  
> *Because the encoder maps both modalities into a **unit sphere**, cosine similarity becomes equivalent to Euclidean distance after scaling, allowing us to use efficient ANN structures designed for Euclidean space without losing semantic fidelity.*  

This geometric shortcut is why large‑scale text‑to‑image search can run in milliseconds on commodity hardware.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
