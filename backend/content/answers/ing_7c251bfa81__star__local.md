---
qid: ing_7c251bfa81__star__local
question: 'Explain: Best Practices — Idempotency | System Design'
topic: Machine Learning
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 237
output_tokens: 361
total_tokens: 598
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-05T03:42:10-05:00'
sources: []
---

**Situation** – In my last role I was building a real‑time recommendation engine for an e‑commerce platform that served personalized product cards to millions of users each day. The API that pushed updates to the model’s feature store was hit by retries from downstream services, and we started seeing duplicate entries in our training dataset, which inflated the model’s bias.

**Task** – I had to design a robust, idempotent ingestion pipeline so that repeated requests would not corrupt the feature store or waste compute resources, while still keeping latency under 200 ms for real‑time scoring.

**Action** – First, I added a deterministic request ID generated by the client and stored it in a Redis cache with a TTL of 24 hours. Each ingestion job checked this cache before writing to the PostgreSQL feature store; if the ID existed, the job was skipped. I also wrapped the database writes in an upsert (INSERT … ON CONFLICT DO UPDATE) so that duplicate keys were safely merged. For auditability I logged every idempotent hit to a CloudWatch stream and set alerts for any spikes. Finally, I automated integration tests using Docker Compose that simulated retry storms and verified no duplicates appeared.

**Result** – After deployment the feature store saw a 99.8 % reduction in duplicate records, model training time dropped by 35 %, and our SLA for real‑time scoring improved to 180 ms on average. I learned that coupling lightweight caching with database upserts is often the simplest, most reliable idempotency pattern in distributed ML pipelines.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
