---
qid: ing_6c5591f635__aws__local
question: 'Explain: Video Processing — System Design Resources'
topic: Machine Learning
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 228
output_tokens: 527
total_tokens: 755
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-07T09:28:47-05:00'
sources: []
---

**Customer Obsession & Ownership**

*Situation:* A media startup needed a real‑time video analytics pipeline for live sports streams (≈ 4K, 60 fps). They required instant object detection and heat‑map generation with < 2 s latency while keeping monthly cost <$10k.

*Task:* Design an end‑to‑end system that scales to 100 concurrent streams, guarantees 99.9% availability, and delivers actionable insights within the latency budget.

*Action:*  
1. **Ingest** – Use *Amazon Kinesis Video Streams (KVS)* with edge ingestion points; each stream is split into 10 ms fragments for low‑latency delivery.  
2. **Processing** – Deploy a fleet of *AWS Lambda@Edge* functions triggered by KVS “fragment arrived” events. Each function runs an optimized TensorFlow Lite model on the same container (ECR) to detect players and scoreboards.  
3. **Aggregation & Storage** – Results are published to *Amazon SNS* topics; downstream services (e.g., DynamoDB Streams) aggregate per‑frame metrics into a *DynamoDB* table with TTL for 24 h retention. Heat‑maps are generated by an *AWS Glue* job that reads from S3 and writes PNGs back to S3, then triggers a *CloudFront* distribution for instant viewer access.  
4. **Observability** – Enable *Amazon CloudWatch Logs Insights* to track latency per function; set alarms on > 1 s thresholds and auto‑scale Lambda concurrency.

*Result:* The prototype handled 120 concurrent streams with < 1.8 s end‑to‑end latency, 99.95% availability, and cost $7.6k/month (≈ $0.13 per processed frame). Compared to a monolithic EC2 solution that would have cost >$25k and had 10× higher latency.

*Learnings:*  
- **Dive Deep** into Lambda concurrency limits revealed hidden throttling; adding a provisioned concurrency pool mitigated spikes.  
- **Bias for Action**: we prototyped with KVS before committing to custom RTMP ingestion, saving weeks of dev time.  

This architecture demonstrates ownership, deep technical trade‑offs, and measurable impact—qualities a bar‑raiser looks for in an AWS system design answer.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
