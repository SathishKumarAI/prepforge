---
qid: ing_696ef7a5c3__faang__local
question: 'Explain: Action-item extraction & the faithfulness gate'
topic: AI
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 232
output_tokens: 472
total_tokens: 704
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-06T22:48:32-05:00'
sources: []
---

**Clarify**  
The interviewer asks for a concise explanation of *action‑item extraction* and its *faithfulness gate*. I’d confirm that we’re talking about NLP pipelines that pull actionable tasks from unstructured text (emails, meeting notes, chat logs) and the mechanism that ensures those extracted actions truly reflect the source content.  

**Approach**  
1. Define action‑item extraction: identify verbs + objects that indicate a task, map to assignee, due date, priority.  
2. Explain faithfulness gate: a verification step that checks the extracted tuple against the original sentence for semantic consistency and absence of hallucination.  

**Depth**  
- **Extraction** uses a two‑stage model: (a) *entity recognition* with BiLSTM‑CRF or transformer fine‑tuned on labeled action data, producing spans like “schedule meeting”, “send report”. (b) *relation extraction* to link verbs to actors and temporal cues via dependency parsing.  
- **Faithfulness gate** employs a *semantic similarity scorer*: a sentence‑pair model (e.g., RoBERTa fine‑tuned on entailment data) that compares the source clause with a synthetic “action summary” generated by a decoder. If the entailment score falls below a threshold, the action is rejected or flagged for human review.  
- Complexity: extraction runs in linear time O(n) per sentence; faithfulness adds constant‑time inference per extracted item.  

**Edge Cases**  
- Ambiguous pronouns (“he’ll do it”) → coreference resolution needed.  
- Implicit actions (“Let’s touch base next week”) → requires contextual inference.  
- Negative statements (“Don’t send the draft”) → need to detect negation scope.  

**Optimize & Communicate**  
I’d suggest caching encoder outputs for reuse in the faithfulness step, reducing GPU cycles. For production, a confidence‑based threshold allows scaling: low‑confidence items go to a human queue, high‑confidence ones auto‑post to task boards. I would narrate this as: “First we pull candidate tasks, then we gate them against their source text to avoid hallucinations—essential for trustworthy AI in enterprise settings.”

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
