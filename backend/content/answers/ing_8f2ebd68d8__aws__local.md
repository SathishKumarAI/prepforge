---
qid: ing_8f2ebd68d8__aws__local
question: Your agent charged a customer's card twice. The trace shows one tool call.
  What happened, and how do you prevent it?
topic: AI
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 244
output_tokens: 444
total_tokens: 688
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-07T10:50:03-05:00'
sources: []
---

**Situation / Task**  
During a recent rollout of our AI‑powered checkout agent, we detected that 12 % of users were charged twice for the same purchase (≈$1.2 M in revenue loss). The stack trace revealed only one call to the payment service – yet two charges appeared.

**Action**  
I led a quick “black‑box” dive:  
* **Root cause:** The agent’s response handler was idempotent‑unsafe; after the initial `CreateCharge` succeeded, a retry on network failure re‑executed the same request because we reused the same payload without an *idempotency key*.  
* **Fixes implemented (AWS services):**  
  - Added a **deduplication ID** generated by a Lambda that hashes `{orderId, amount, timestamp}`.  
  - Stored the key in **DynamoDB** with TTL = 24 h to guard against replay attacks.  
  - Wrapped the payment call inside an **SQS FIFO queue** (visibility timeout = 30 s) so only one consumer processes a given key.  
  - Updated the Lambda to return a *deduplication‑aware* response and to log the key in CloudWatch for auditability.

**Result**  
Post‑deployment, duplicate charges dropped from 12 % to <0.01 % (≈$10 k). The new flow added <$1 / charge in cost while improving availability (SQS FIFO guarantees at‑least‑once delivery with order preservation).  

**Learning & Ownership**  
I documented the failure mode and added a unit test that simulates transient network errors, ensuring future releases maintain idempotency. This demonstrates ownership, deep technical dive, and bias for action—key Amazon principles I bring to every customer‑facing system.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
