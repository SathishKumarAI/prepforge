---
qid: ing_0bd51c9949__faang__local
question: 'Explain: 2.2 On-Policy Distillation — Rethinking On-Policy Distillation
  of Large Language Models: Phenomenology, Mechanism, and Recipe'
topic: Machine Learning
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 257
output_tokens: 574
total_tokens: 831
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-06T19:11:36-05:00'
sources: []
---

**Clarify**  
We’re asked to explain *On‑Policy Distillation* for large language models (LLMs): why it matters, how it works, and what the practical recipe looks like. I’ll assume “on‑policy” means the student is trained on data generated by its own policy during distillation, not a fixed dataset.

**Approach**  
1. Define distillation in RL/LLM context.  
2. Contrast off‑policy vs on‑policy settings.  
3. Describe the phenomenology (what we observe).  
4. Outline the mechanism (algorithmic steps).  
5. Give a concise recipe with key hyper‑parameters.

**Depth**  

| Aspect | Detail |
|--------|--------|
| **Phenomenology** | Students converge faster, retain teacher’s exploration bias, and adapt to distributional drift as they generate their own prompts. They also inherit the teacher’s calibration without extra regularization. |
| **Mechanism** | 1️⃣ Teacher samples a trajectory (prompt–response pairs). 2️⃣ Student receives same prompt, generates response via its policy. 3️⃣ Compute KL loss between student logits and teacher logits; add entropy bonus for exploration. 4️⃣ Update student with gradient‑based RL objective (e.g., PPO or REINFORCE) on the generated trajectory. |
| **Recipe** | - *Teacher*: frozen pre‑trained LLM. <br>- *Student*: smaller model initialized randomly. <br>- *Batch size*: 32–64 trajectories. <br>- *Learning rate*: \(1e{-4}\). <br>- *KL weight*: 0.05–0.1. <br>- *Entropy bonus*: 0.01. <br>- *Training steps*: until validation loss plateaus (≈ 10k steps). |
| **Complexity** | Each step is O(L×V) where L = sequence length, V = vocab; dominated by forward passes of both teacher and student. Memory grows with batch size. |

**Edge Cases**  
- Teacher’s bias → student over‑fits to narrow style; mitigate via temperature scaling.  
- Distribution shift: if student generates out‑of‑distribution prompts, KL loss becomes large—use gradient clipping.  
- Degenerate policy (always repeats teacher output): entropy bonus keeps exploration.

**Optimize & Communicate**  
Explain that on‑policy distillation leverages the teacher’s experience while letting the student learn from its own mistakes, leading to a more robust LLM with fewer parameters. Highlight trade‑offs: higher compute during training vs lighter inference. Conclude by stressing that careful tuning of KL weight and entropy bonus is critical for stable convergence.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
