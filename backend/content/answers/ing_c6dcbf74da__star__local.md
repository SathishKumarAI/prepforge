---
qid: ing_c6dcbf74da__star__local
question: Design the memory and personalisation layer for an assistant serving millions
  of users. What do you store, when do you summarise versus retrieve, and how do you
  evaluate memory quality?
topic: AI
model: openai/gpt-oss-20b
provider: lmstudio
input_tokens: 261
output_tokens: 399
total_tokens: 660
web_searches: 0
cost_usd: 0.0
generated_at: '2026-09-05T06:07:32-05:00'
sources: []
---

**Situation** – At my last role I built a conversational AI for a global customer‑support platform that had over 5 million active users. The product needed to remember past interactions and personalize replies without blowing up storage or latency.

**Task** – Design a scalable memory & personalisation layer: decide what data to persist, how often to summarise versus fetch raw logs, and how to measure the quality of the remembered context so that each user gets consistent, relevant answers within 200 ms.

**Action** – I implemented a two‑tier system. Tier 1 stores fine‑grained event streams (utterances, intent tags, timestamps) in an append‑only columnar store (ClickHouse) with per‑user sharding for fast point lookups. Tier 2 holds compressed user embeddings generated by a transformer encoder that ingests the last 20 turns; these are updated nightly via incremental training and stored in a key‑value cache (Redis). Retrieval logic first checks Redis for a “context snapshot”; if stale or missing, it pulls recent events from ClickHouse to re‑embed. Summaries are triggered when user sessions exceed 50 interactions or after 24 h of inactivity, using an extractive summariser that preserves intent and sentiment tokens. To evaluate memory quality I run A/B tests comparing answer relevance scores (BLEU + human NPS) against a control with no memory; we also monitor drift by measuring cosine similarity between successive embeddings.

**Result** – The system cut average response latency from 350 ms to 180 ms, improved user satisfaction NPS from 78 to 87, and reduced storage costs by 40% through on‑demand summarisation. I learned that a hybrid fine‑grained/embedding approach balances fidelity with performance, and continuous quality metrics are essential for maintaining long‑term user trust.

_Written by openai/gpt-oss-20b running locally. Machine-generated, not reviewed._
